# Optional. Without a token the globe uses Cesium's bundled offline Natural Earth II
# imagery, which works with no account and no network. With a token you get
# high-resolution world imagery and terrain.
#
# Get one at https://ion.cesium.com, then copy this file to .env.local
VITE_CESIUM_ION_TOKEN=
